# Cookidoo account credentials
COOKIDOUGH_EMAIL=you@example.com
COOKIDOUGH_PASSWORD=change-me

# Locale. COUNTRY is ISO 3166-1 alpha-2. LANGUAGE accepts either a bare
# ISO 639-1 code (de, en) which is paired with COUNTRY automatically, or an
# explicit BCP-47 tag (de-DE, en-GB). Case is normalized internally.
COOKIDOUGH_COUNTRY=de
COOKIDOUGH_LANGUAGE=de

# Transport: stdio (default) or http
COOKIDOUGH_MCP_MODE=stdio
COOKIDOUGH_MCP_HOST=127.0.0.1
COOKIDOUGH_MCP_PORT=8765

# Required for COOKIDOUGH_MCP_MODE=http instead of EMAIL/PASSWORD above:
# multi-tenant OAuth 2.1 (see README "Multi-tenant HTTP / OAuth 2.1").
#COOKIDOUGH_PUBLIC_URL=https://your-app.up.railway.app
#COOKIDOUGH_DATABASE_URL=postgres://user:password@host:5432/db
#COOKIDOUGH_ENCRYPTION_KEY=

# Minimum quality score to allow custom-recipe upload (0-100)
COOKIDOUGH_QUALITY_BAR=70

# Optional: persist session cookies across restarts (skips the OAuth2 login
# while they are valid). The file holds live session credentials — keep it
# private (written with 0600 permissions, excluded via .gitignore).
#COOKIDOUGH_COOKIES_FILE=cookies.json
